import styled from "styled-components";
import { AppColor, AppFonts } from "../styled/global";

export const HeadingTitle = styled.h1`
  font-family: ${AppFonts.heading};
  font-style: normal;
  font-weight: normal;
  font-size: 25px;
  line-height: 37px;
  letter-spacing: 1.13636px;

  color: ${AppColor.headingTitle};
`;
